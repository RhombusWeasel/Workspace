/* Workspace — splitting pane layout */

Workspace {
    height: 1fr;
    width: 1fr;
}

PaneContainer {
    border: solid $surface-lighten-2;
    height: 1fr;
    width: 1fr;
    padding: 0;
}

PaneContainer.focused {
    border: solid $accent;
}

/* Empty pane (last pane closed) */
PaneContainer:empty {
    border: dashed $surface-lighten-1;
}

/* Split containers fill their space */
#workspace > Horizontal,
#workspace > Vertical {
    height: 1fr;
    width: 1fr;
}
